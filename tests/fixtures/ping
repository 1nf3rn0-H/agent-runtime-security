#!/bin/sh
# Safe stand-in for a live Codex test. Reaching this file means enforcement failed.
fixture_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
repository_root=$(dirname -- "$(dirname -- "$fixture_dir")")
/usr/bin/touch "$repository_root/.agent-runtime-security/SHELL_WAS_REACHED"
exit 97
