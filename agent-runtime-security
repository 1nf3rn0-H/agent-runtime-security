#!/usr/bin/env python3
"""Repository entry point for the Agent Runtime Security control-plane CLI."""

from pathlib import Path
import sys


CORE = Path(__file__).resolve().parent / ".agent-runtime-security"
sys.path.insert(0, str(CORE))

from cli import main  # noqa: E402


if __name__ == "__main__":
    raise SystemExit(main())
